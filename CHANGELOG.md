# 修订记录

本工程的修订记录。版本号遵循语义化版本：破坏性变更提升主版本，新增功能提升
次版本，修复提升修订号。

## [1.0.0] - 2026-09-15

首次发布。

### 显示

- ST77922 QSPI 显示驱动，320×480，RGB565，80 MHz 像素时钟。
- 初始化序列 63 条，含 `CASET` / `RASET` 的窗口地址设置。
- 高低字节交换的 RGB565 输出。`data_endian` 配置项只被 ST7789 驱动读取，
  ST77922 驱动不读，因此在数据打包处完成交换。
- 不使用复位引脚：板上显示复位与 EN 共线，复位阶段会一并复位主控。
- 横带渲染：整屏按 32 行分带，逐带绘制并推送。绘制缓冲为单块
  `320 × 32 × 2 = 20,480 B`，不超过总线单次传输上限 `max_transfer_sz`
  （`320 × 80 × 2 = 51,200 B`），避免整屏双缓冲的静态占用。

### 绘图

- `gfx` 组件：跨度感知画布、裁剪、矩形填充、8×8 字形渲染。
- 字形渲染按跨度写入，不逐像素调用。
- 提供面板颜色构造函数，集中处理 RGB565 的字节序。
- 文本与字形的契约在头文件写明：越界静默裁剪，字形表仅覆盖 ASCII。

### 场景

- 12 个场景：`orient`、`selftest`、`solid`、`pattern`、`edge`、`text`、
  `scroll`、`backlight`、`touch`、`rgb`、`audio`、`status`。
- 场景经 `enter` / `exit` 钩子建立与还原硬件状态，离开场景不残留改动。
  凡是进入时改动且不会自行恢复的硬件状态，都必须在离开时还原。
- 后 4 个场景随对应子系统开关参与编译。

### 子系统

- 背光：LEDC PWM，5 kHz，10 位。
- 按键：BOOT 键轮询与去抖，场景切换。
- I2C 总线：由 `bsp_i2c` 统一持有，触摸与音频共用。
- 触摸：ST77922 内置控制器，I2C 0x55，最多 5 点，协议逐项对应厂商库寄存器定义。
- RGB：WS2812B，经 `led_strip` 组件驱动，不手写位时序。
- 音频：ES8311 + 功放，经 `esp_codec_dev` 组件驱动，不手写寄存器；
  16 kHz 立体声 16 位，正弦表查表合成，相位为整数累加。
- 电池：ADC1 通道 7，2:1 分压，曲线拟合校准，多次采样取平均。
- MicroSD：SDIO 四位，挂载于 `/sdcard`，不自动格式化。

### 门控

- 五个可选子系统由 Kconfig 门控，默认关闭。关闭时本工程自身对应的驱动与场景
  源码不参与编译与链接；`idf_component.yml` 声明的第三方组件不受 Kconfig 门控，
  在三套配置下都会被获取、编译并作为链接输入，未引用段由 `--gc-sections` 丢弃。
- 必需子系统（显示、背光、按键、I2C 总线）初始化失败即中止启动；
  可选子系统失败仅告警降级，避免硬件缺省导致整机无法启动。
- 复合条件（I2C 总线、`status` 场景）收敛为派生 Kconfig 符号，避免在 CMake 与
  C 侧重复书写同一条件而漏改。
- 三套构建配置：出厂（全关）、全子系统（全开）、混合（仅触摸与 MicroSD）。
  出厂与全开只覆盖门控的两个角，混合配置让两个复合条件各由单侧满足。

### 测试

- `gfx` 宿主侧单元测试：画布裁剪、跨度边界、字形位序与行序、放大倍数。
- `tools/preview` 宿主侧场景渲染与校验，与设备共用同一份场景源码：
  全部 8 个场景做整屏覆盖校验，3 个动画场景做帧推进校验，带 `exit` 钩子的
  场景做退出还原校验，`pattern`、`edge`、`orient`、`text` 另有逐像素布局断言；
  输出字符图与 24 位 BMP，可先推进若干帧再渲染。
- `scripts/check.sh` 本地门禁：宿主侧测试 + 三套配置构建。

### 修复

- `vTaskDelay(pdMS_TO_TICKS(5))` 在默认 100 Hz tick 频率下等于
  `vTaskDelay(0)`，主任务不让出 CPU，空闲任务饿死并触发任务看门狗。
  改为至少延时 1 个 tick。
- 离开场景不还原硬件状态：RGB 灯色、背光亮度、音频输出会一直残留。
  引入 `exit` 钩子。
- 音频停止改用静音而非关闭编解码。关闭会禁用 I2S 通道，下一次打开时
  `esp_codec_dev` 的 `set_fmt` 会对已禁用的通道再次调用 `i2s_channel_disable`，
  IDF 驱动据此打出错误日志。
- 触摸驱动原本自行创建 I2C 总线，与音频共用同一组引脚时会冲突。
  改为由 `bsp_i2c` 统一持有总线。
- 宿主侧测试脚本未与 ESP-IDF 交叉工具链隔离：在已激活 ESP-IDF 的 shell 中
  运行时，PATH 里的交叉汇编器会被宿主的 `cc` 找到并以 `--64` 调用，导致
  连编译探测程序都失败。改为只保留宿主侧工具目录。
- 构建脚本在 Espressif 安装管理器的激活方式下找不到 `idf.py`：该方式把
  `idf.py` 定义成含点号的 shell 函数，bash 不把这类函数名传给子进程。
  改为在 `idf.py` 不可见时用可继承的 `IDF_PATH` 与 `IDF_PYTHON_ENV_PATH`
  直接调用。`flash.sh` 原本写死带 IDF 版本号的 venv 路径，同批改为同一解析顺序。
- 屏初始化的计时口径错误：原先把整个 `bsp_init()` 的耗时当作屏初始化耗时上报，
  而该函数还包含可选子系统的初始化（触摸有两段固定 100 ms 复位延时，MicroSD
  无卡时也要走一次挂载尝试），全子系统配置下必然超过 300 ms 的验收判据。
  改为单独对 `bsp_display_init()` 计时。
- `scene_edge` 的边位置数组未按 y 升序，而填充逻辑依赖其单调递增：相邻间隔
  出现负高度被跳过，黑白交替相位错开，六条标注边中的两条消失，诊断场景失效。
  改为升序并注明该数组必须有序。
- `scene_rgb` 用中文名作档位名称，而字形表只覆盖 ASCII 0x00–0x7F，
  非 ASCII 字节被掩码成无关字形，实机上显示为乱码。改为英文名。
- `bsp_battery` 的无校准回退分支把 2:1 分压比乘了两次，读数为真实值的两倍。
  分压比改为只施加一次。
- `bsp_audio` 在编解码创建失败后每次进入音频场景都重试创建，而创建路径会分配
  对象，造成持续泄漏。改为资源创建只尝试一次。
- `scene_status` 有一行 49 字符超出面板宽度，被静默裁掉。缩短该行。
- `components/bsp/CMakeLists.txt` 的 `REQUIRES` 去掉遗留伞组件 `driver`，
  改为列出实际使用头文件所属的组件（原先还声明了本组件并未使用的 `esp_timer`，
  该头文件只在 `main` 中使用）。去除 `driver` 不改变构建图（`esp_lcd` 依赖
  `driver`），但让本组件声明的依赖与实际一致。

### 已记录但未消除的限制

- 面板对强对比水平边缘有轻微串扰。
- QSPI 像素时钟 80 MHz 高于数据手册写模式上限 62.5 MHz。
- MicroSD 未实机验证。
- 电池电量判据未验证（无电池）。
- 背光 PWM 的调光范围与低占空比下的闪烁未验证。
- 按键为电阻上拉轮询，音频场景同步写 I2S 期间（约 300 ms）的按压会被漏掉。
